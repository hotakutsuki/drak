using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameVariables : MonoBehaviour
{

    public enum Direction { North, East, South, West };
    public enum TileStates { InGame, Positioning, InCanvas, InDeck};
    public enum PlayerCards { Knight, Rager, Druid };
    //public static ArrayList directions = new ArrayList<String>() { { add("asd") } } ["North", "East", "South", "West "];

}
